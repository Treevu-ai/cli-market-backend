# Shared core library — single source of truth for the data-moat modules.
# Pinned to a specific commit so the collector deploy is reproducible.
cli-market-core @ git+https://github.com/Treevu-ai/cli-market-core@74c5ada53a5270b9d845fceff942fd6fca4dc326

# Semantic moat — Golden Record persistence (SQLite dev / Postgres prod).
cli-market-index[postgres] @ git+https://github.com/Treevu-ai/cli-market-index@2bfa6d210fc429e1c9c7780d7df9158eb0713a66

# Runtime / dev deps not pulled in transitively by cli-market-core.
playwright>=1.45.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0