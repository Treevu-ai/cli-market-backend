[project]
name = "cli-market-backend"
version = "1.0.0"
description = "CLI Market backend — collector, connectors, indicators, enrichment"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "httpx>=0.27",
    "asyncpg>=0.29",
    "aiosqlite>=0.20",
    "pydantic>=2.7",
]
